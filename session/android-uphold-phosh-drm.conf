[Unit]
# Shadows /usr/lib/.../android-service@hwcomposer.service.d/20-phosh.conf:
# uphold the DRM phosh session instead of stock phosh. (Stock phosh remains
# available as phosh-drm's OnFailure fallback.)
Upholds=phosh-drm.service
